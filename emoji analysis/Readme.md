# Emoji Analysis (PGR Reproduction)

This module analyzes emojis from YouTube comments from both songs, and it computes
emoji frequencies (overall and per year), and generates summary figures.

**Environment:** `project-main-release-Yeliz-Yucel`  
Create it from the repo root with:
```bash
conda env create -f environment.yml -n project-main-release-Yeliz-Yucel
conda activate project-main-release-Yeliz-Yucel
